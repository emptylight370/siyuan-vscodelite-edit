# Project Context Card Template

Use this template when generating `project-context.md`. Rules:

- The agent **auto-fills the card by inspecting the repository** (README / CONTRIBUTING, directory structure, CI configuration, issue templates, `gh label list`, etc.); never substitute guesses for facts;
- Mark fields that cannot be inferred from the repository as `pending` and list them in the report;
- The **user preferences** field does not come from repository inspection - always obtain it **by asking the user**, then fill it in;
- Draft → user review → write to `project-context.md` in the SKILL.md directory.

---

```markdown
# <owner/repo> Issue Triage Context Card

> Generated by the issue-triage skill on <date>, reviewed by the user.

## Repository Info

- **owner/repo**:
- **Default branch**:
- **Issue language convention**: (language to use when replying to issues)

## Issue Label List

> Pulled from `gh label list`. Labeling may only use labels from this list; creating a new label requires user consent first, followed by updating this list.

| Label | Semantic description |
| ----- | -------------------- |
|       |                      |

## Project Conventions

- **Code structure / key directories**:
- **Mandatory checklists for changes**: (e.g. schema five-step checklist)
- **Lint command**:
- **Test command**:

## Change Red Lines

<!-- Files/behaviors that must not be touched, e.g. config schema versions, release-related files -->

-

## Closure Strategy

- **When an issue may be closed**: (immediately / after verification / after release)
- **Intermediate-state label**: (must exist in the label list above, e.g. pending-feedback)

## Commit Strategy

<!-- Delivery form for code changes: direct commit, or new branch with a PR; default to PR when undeclared -->

## User Preferences

<!-- Obtained by asking the user, not from repository inspection; applied directly on subsequent runs without re-asking -->

- **Progress tracking**: (enabled / disabled)
- **Progress directory**: (one progress file per issue, e.g. .codebuddy/issue-triage/progress/; default suggestion: a subfolder named after the skill under the running tool's data subdirectory)
- **Other preferences**:
```
